# Dev

```sh
# TODO: move these to config.yaml and let server render these.
% echo "{}" > client/src/js/actions/remote/firebase-config.json
```

```sh
% yarn install
% go run main.go & yarn start
```
